// Doctor profile cards — placeholder for hair (hair Clinic lacks doctor_stats yet).
// Returns null until doctor data lands on hair's Clinic type.

import type { Clinic } from "@/lib/types";

export default function DoctorProfile({ clinic: _c }: { clinic: Clinic }) {
  return null;
}
